import SwiftUI

struct StreamLogView: View {
    var body: some View {
        VStack {
            // Future content
        }
        .navigationTitle("Histórico")
    }
}

#Preview {
    StreamLogView()
}
