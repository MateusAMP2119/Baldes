import SwiftUI

struct StreamLogView: View {
    var body: some View {
        VStack {
            Text("Histórico")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

#Preview {
    StreamLogView()
}
